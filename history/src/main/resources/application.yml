server:
  port: 8082

spring:
  data:
    mongodb:
      database: history-db
      #host: localhost
      port: 27017
      host: docker-mongo
      username: rootuser
      password: rootpass
      authentication-database: admin


