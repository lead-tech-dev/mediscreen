server:
  port: 8082

spring:
  data:
    mongodb:
      database: history-db
      port: 27017

      #host: docker-mongo
      #username: rootuser
      #password: rootpass
      #authentication-database: admin


